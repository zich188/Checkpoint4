<form method="post" action="{{ path('jet_delete', {'id': jet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jet.id) }}">
    <button class="btn">Delete</button>
</form>
